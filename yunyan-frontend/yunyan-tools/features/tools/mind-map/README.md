# Mind Map Tool

## Purpose
- Convert topic outlines into mind maps with local-first rendering/export.
- Reserve contract points for backend persistence and generation.

## Module Structure
- `components/workspace`: workspace UI and sections.
- `constants`: preset and defaults.
- `services`: API adapter and runtime contract.
- `types`: tool types.

## Runtime
- Runtime contract export: `mindMapRuntimeContract`.
